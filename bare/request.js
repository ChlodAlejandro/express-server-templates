module.exports = async (req, res) => {
    // Edit this function!
    res.set("Content-Type", "text/plain")
    res.send(
        "Hello World! Looks like you got that template running!\r\n"
        + "You should go ahead and change `request.js` now to suit your needs.");
};